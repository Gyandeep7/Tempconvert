const celsiusToFahrenheit = (celsius: number) => (celsius * 9/5) + 32;
const sqYardToSqFt = (sqYard: number) => sqYard * 9;
const cmToM = (cm: number) => cm / 100;

export { celsiusToFahrenheit, sqYardToSqFt, cmToM };
